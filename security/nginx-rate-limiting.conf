# Custom Nginx configuration for Nginx Proxy Manager
# Place in: /data/nginx/custom/http.conf (mounted at http-level)
limit_req_zone $binary_remote_addr zone=immich_login:10m rate=5r/m;

# Per-host "Advanced" tab config in NPM UI:
# client_max_body_size 0;
# proxy_read_timeout 600s;
# proxy_send_timeout 600s;
#
# location /api/auth/login {
#     limit_req zone=immich_login burst=5 nodelay;
#     proxy_pass http://YOUR_INTERNAL_HOST:YOUR_PORT;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
